# Environment Variables – Miner Bitaxe Dashboard

All variables are expected to be loaded from a `.env` file in the `backend/` directory.

## Miner and external APIs

BITAXE_BASE_URL=http://192.168.1.132
COINGECKO_API_BASE=https://api.coingecko.com/api/v3
COINGECKO_API_KEY=

## Database

DATABASE_URL=postgresql+psycopg2://postgres:password@localhost:5432/bitaxe_db
