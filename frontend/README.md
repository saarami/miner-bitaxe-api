
# Frontend – Bitaxe Dashboard UI (React + Vite)

## Setup

```bash
cd frontend
npm install
```

## Run in development

```bash
npm run dev
```

This assumes your backend (FastAPI) is running on `http://localhost:8000`.

The Vite dev server proxies `/api` calls to the backend (see `vite.config.js`).

Open the app at the URL that Vite prints (usually `http://localhost:5173`).
